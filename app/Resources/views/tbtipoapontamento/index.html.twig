{% extends ':template:template.html.twig' %}


{% block stylesheets %}

    {{ parent() }}

    <link rel="stylesheet" href="{{ asset('bundles/saatendimento/css/dataTables.bootstrap.css') }}">

{% endblock %}

    {% block contentheader %}


        <section class="content-header">
            <h1>
                Tipo de Apontamento
                <small>Listar</small>
            </h1>
        </section>


    {% endblock %}


    {% block panelheading %}
        <a href="{{ path('tipoapontamento_new') }}" class="btn btn-primary btn-sm">
            <span class="glyphicon glyphicon-plus"></span>
            Novo
        </a>
    {% endblock %}

    {% block panelbody %}
        <div class="table-responsive">
            <table class="table table-bordered table-condensed table-hover table-striped table-datatable">
                <thead>
                <tr>
                    <th>Opções</th>
                    <th>Tipo de Atendimento</th>
                    <th>Descrição</th>
                    <th>Status</th>
                </tr>
                </thead>
                <tbody>
                {% for tbTipoApontamento in tbTipoApontamentos %}
                    <tr>
                        <td>
                            <a href="{{ path('tipoapontamento_show', { 'id': tbTipoApontamento.tapCodigo }) }}" class="btn btn-primary btn-xs">
                                <span class="glyphicon glyphicon-eye-open"></span>
                            </a>
                            <a href="{{ path('tipoapontamento_edit', { 'id': tbTipoApontamento.tapCodigo }) }}" class="btn btn-primary btn-xs">
                                <span class="glyphicon glyphicon-edit"></span>
                            </a>
                        </td>
                        <td>{{ tbTipoApontamento.atCodigo.atDescricao }}</td>
                        <td>{{ tbTipoApontamento.tapDescricao }}</td>
                        <td>
                            {% if tbTipoApontamento.tapStatus == 1 %}
                                Ativo
                            {% else %}
                                Inativo
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>


    {% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('bundles/saatendimento/js/jquery-datatable.js') }}"></script>
    <script src="{{ asset('bundles/saatendimento/js/dataTables.bootstrap.js') }}"></script>
    <script src="{{ asset('bundles/saatendimento/js/mrs-dataTable.js') }}"></script>

{% endblock %}